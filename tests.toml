#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    args.image = "nginx:alpine"
    args.container_port = 80
    args.restart_policy = "no"
    args.use_data_volume = false
    args.docker_options = ""
